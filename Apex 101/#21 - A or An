// #21 - A or An
// https://www.apexsandbox.io/problem/21

public String aOrAn(String word) {
    
    if (word.startsWith('a') || 
        word.startsWith('e') || 
        word.startsWith('i') || 
        word.startsWith('o') || 
        word.startsWith('u')) {
        return 'an ' + word;
    } else {
        return 'a ' + word;
    }
}